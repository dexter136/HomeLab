credentials:
  existingSecret: cloudflared-credentials
config:
  tunnel: homelab
  ingress:
    - hostname: '*.dex136.xyz'
      service: https://ingress-nginx-external-controller.network
      originRequest:
        noTLSVerify: true
    - service: http_status:404

podMonitor:
  enabled: true
  metricsEndpoints:
    - port: http
